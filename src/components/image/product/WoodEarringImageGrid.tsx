"use client";

import {WoodEarringImageData} from "@/data/imageData";
import ImageGridWithLightbox from "@/components/image/ImageGridCommon";

const WoodEarringImageGrid = () => {
  return <ImageGridWithLightbox images={WoodEarringImageData}/>;
};

export default WoodEarringImageGrid;
